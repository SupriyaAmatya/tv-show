export const base = 'https://api.tvmaze.com';
export const showsEndpoint = `${base}/shows`;
export const scheduleShowsEndpoint = `${base}/schedule?country=US`;
export const episodesEndpoint = `${base}/episodes`;